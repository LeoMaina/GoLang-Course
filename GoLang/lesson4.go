package main

import "fmt"

func main() {
	congrats := "happy birthday" //Declaring a variable named "congrats" of type string
	number := 10                 //Declaring a variable named "number" of type int and initialising it to a value "10"
	state := true

	fmt.Println(congrats, number, state)
}
